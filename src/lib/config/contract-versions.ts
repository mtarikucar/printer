/**
 * Versions of the partner agreements.
 *
 * Bump the version whenever the corresponding text in src/lib/content/ changes
 * materially, and keep the version line inside the text itself in sync. The
 * accepted version is written to `onboarding_version` at registration so a
 * later edit can never be claimed against a partner who signed an older text.
 */
export const MANUFACTURER_CONTRACT_VERSION = "2.0";
export const PAINTER_CONTRACT_VERSION = "2.0";
